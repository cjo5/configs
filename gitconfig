[alias]
    br = branch
    co = checkout
    cm = commit
    rs = reset
    st = status

    tree = log --graph --decorate --pretty=oneline --abbrev-commit

[color]
    ui = auto

[core]
    editor = /usr/bin/vim
    excludesfile = ~/.gitignore

[push]
    default = current

[user]
    name = Carl Johnell
